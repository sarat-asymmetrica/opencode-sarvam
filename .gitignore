# Secrets / environment
.env
.env.local
*.secret

# Node / JavaScript
node_modules/
package-lock.json
.npm/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# OpenCode local state
.opencode/logs/
.opencode/cache/
.opencode/state/
*.log

# IDE / editor noise
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.exe
*.o
*.out

# Language-specific build artifacts from exercise runs
dr-classify/dr-classify.exe
dr-classify/*.test

# SWE-bench thesis research: upstream repos cloned for study (not forked)
# See INFRA_STACK.md — these are read-only study targets, NOT our code
research/

# Session state — durable but not version-controlled
# (todos, memory, etc. — survives compaction but not across git clones)
.opencode/state/
.gitnexus

.gitnexus
